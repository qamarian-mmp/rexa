package main

const (
	mKNL_DEL_BUFFER_SIZE int = 1 /* Buffer size of the channels the kernel and a delegate use to
		communicate. */
	mDEL_THR_BUFFER_SIZE int = 1 /* Buffer size of the channels a delegate and its thread use to
		communicate. */
)
